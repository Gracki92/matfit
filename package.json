{
  "name": "matfit-pro",
  "version": "0.25.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/domain/backup.js && node --check src/domain/date.js && node --check src/domain/nutrition.js && node --check src/domain/planner.js && node --check src/domain/products.js && node scripts/check-project.mjs && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
